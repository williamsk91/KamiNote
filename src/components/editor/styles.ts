import { css } from "styled-components";
import { colors } from "components/styles/colors";

export const marksStyles = css`
  code {
    padding: 0 3px;
    border-radius: 3px;

    color: red;
    background-color: ${colors.clouds};
  }
`;
